# ============================================
# DATABASE (Neon PostgreSQL)
# ============================================
# Production/Staging database connection string
# Get this from: Neon Dashboard → Project → Connection Details
DATABASE_URL="postgresql://user:password@ep-xxx.region.aws.neon.tech/gym_db?sslmode=require"

# Development database connection string (used when NODE_ENV=development)
# This allows using a separate database for local development
# If not set, falls back to DATABASE_URL
DEV_DATABASE_URL="postgresql://user:password@ep-xxx.region.aws.neon.tech/gym_dev?sslmode=require"

# ============================================
# AUTHENTICATION
# ============================================
# JWT secret for session tokens (minimum 32 characters)
# Generate with: openssl rand -base64 32
JWT_SECRET="your-super-secret-jwt-key-minimum-32-characters-long"

# ============================================
# AI SERVICES (Anthropic Claude)
# ============================================
# Get from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY="sk-ant-api03-your-key-here"

# ============================================
# APPLICATION
# ============================================
# Your application URL (no trailing slash)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Environment indicator (development | staging | production)
NODE_ENV="development"

# ============================================
# PAYMENTS (Stripe) - Optional
# ============================================
# Test keys for development, live keys for production
# Get from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_..."

# ============================================
# MONITORING (Optional)
# ============================================
# Sentry DSN for error tracking
# Get from: https://sentry.io → Project Settings → Client Keys
SENTRY_DSN="https://...@sentry.io/..."
SENTRY_AUTH_TOKEN="..."

# ============================================
# ENVIRONMENT-SPECIFIC NOTES
# ============================================
#
# LOCAL DEVELOPMENT:
#   - Use local PostgreSQL or Neon dev branch
#   - NODE_ENV=development
#   - NEXT_PUBLIC_APP_URL=http://localhost:3000
#
# STAGING (Vercel Preview):
#   - Use dedicated staging database
#   - NODE_ENV=staging (or production for Vercel)
#   - NEXT_PUBLIC_APP_URL=https://staging.yourdomain.com
#   - Use test Stripe keys
#
# PRODUCTION (Vercel Production):
#   - Use production database with backups enabled
#   - NODE_ENV=production
#   - NEXT_PUBLIC_APP_URL=https://yourdomain.com
#   - Use live Stripe keys
#   - Use different JWT_SECRET than staging!
#
